<ul class="list-group">
  <% users.each do |user| %>
      <li class="list-group-item list-group-item-info tweet-head">
        <%= image_tag(user.avatar.url(:thumb)) %>
        <span class="panel-title"> <%= link_to user.name, user %> </span>
      </li>
  <% end %>

</ul>